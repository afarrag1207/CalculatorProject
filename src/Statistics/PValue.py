from Statistics.Zscore import zscore

def P_value(data):
    data = []
    n = len(data)
    score = zscore()
    for index in n:
        if n == score:
            data.append(index)

    return index

