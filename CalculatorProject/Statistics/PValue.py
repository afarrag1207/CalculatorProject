from Statistics.Zscore import zscore

def pvalue(data):
    data = []
    n = len(data)
    score = zscore()
    for index in n:
        if n == score:
            data.append(index)

    return index

